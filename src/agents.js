/*import express from 'express';

import { getroadAssistanceProviders_agent, getroadAssistanceProviders_agency, getroadAssistanceProviders_phone} from './agency_collection';

const app = express();
const port = 3000;

app.use(express.json());

app.get('/agent', getroadAssistanceProviders_agent);
app.get('/agency', getroadAssistanceProviders_agency);
app.get('/agent/:phone', getroadAssistanceProviders_phone);


app.listen(port, () =>{
    console.log(`Server is running on port ${port}`)
});*/